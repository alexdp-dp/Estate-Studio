{
  "name": "estate-studio",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix client install --include=dev && npm --prefix server install",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  }
}
